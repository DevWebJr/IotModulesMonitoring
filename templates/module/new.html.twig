{% extends 'layouts/base.html.twig' %}

{% block title %}Ajouter un module{% endblock %}

{% block body %}
    <a href="{{ path('app_module_index') }}">revenir à la liste</a>
    <div class="container-fluid d-flex justify-content-center mt-5 rounded-3 border-primary data-display-box p-5 data-form">
        <h1>Ajouter un module</h1>
        <div class="container">
            {{ include('module/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
